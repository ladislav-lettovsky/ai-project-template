---
description: Core conventions for this project (always active)
alwaysApply: true
---

# Core conventions — always on

## Source of truth for style and quality

Linting, formatting, and type checks are enforced by the repo's tooling,
not restated here. Consult these files instead of guessing:

- `pyproject.toml` — ruff + ty configuration
- `.pre-commit-config.yaml` — local commit gate
- `.github/workflows/ci.yml` — CI gate
- `justfile` — canonical commands

## Commands — use `just`, not raw tools

- `just fmt` — format
- `just lint` — lint (use `just lint-fix` to auto-fix)
- `just type` — type check (uses Astral's `ty`, not mypy)
- `just test` — run tests
- `just check` — full quality gate (what CI runs)

Prefer `just` recipes over invoking `uv run ruff`, `uv run ty`, etc. directly —
the recipes are the contract between dev machines and CI.

## Before claiming a change is done

1. `just check` passes locally
2. Any new public function has a test and a type-annotated signature
3. No new `print()` calls in production code paths — use
   `logger = logging.getLogger(__name__)`
4. Diff looks like what you'd want in a PR review

## Branch and commit discipline

- Never commit directly to `main`
- Branches: `feat/<name>`, `fix/<name>`, `refactor/<name>`, `chore/<name>`, `docs/<name>`, `test/<name>`
- Conventional Commits format for messages
- One logical change per commit
- After any update to local `main` (PR merge, `git pull`, fast-forward, etc.),
  immediately `git switch` to a parking branch — by convention `scratch`,
  forked from the new `main` tip. This keeps the working copy off `main`
  so no edits or commits land there by accident. Create real work
  branches from `main`, not from `scratch`. If `scratch` is missing or
  stale, recreate it with `git switch -c scratch` from the current `main`.
- **First step on a new implementation prompt while on `scratch`:** classify
  the prompt as a new capability (→ `spec/<slug>`) or a fix (→ `fix/<slug>`)
  and rename the parking branch *before* any other action:
  `git branch -m scratch spec/<slug>` (or `fix/<slug>`). The
  `check_branch_name.py` hook rejects prompts on any branch other than
  `main`, `spec/*`, or `fix/*`.

## Ephemeral / scratch work

- Use `.scratch/` at repo root for throwaway files (git-ignored contents)
- Never place exploratory scripts at repo root

## See also

@AGENTS.md
