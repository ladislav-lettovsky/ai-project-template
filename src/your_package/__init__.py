"""your_package — TODO: replace with one-sentence description.

After forking:
1. Rename this directory from `your_package` to your actual package name
   (snake_case, matches the slug of `pyproject.toml [project.name]`)
2. Update `[tool.hatch.build.targets.wheel] packages` in `pyproject.toml`
3. Update the import in `tests/test_smoke.py`
4. Update the wheel-target path in `.claude/settings.json` Edit/Write rules
"""
